# This workflow was the Speechify recruiting grader.
# It is disabled in this portfolio copy as it requires
# access to Speechify's private grading infrastructure.
# See ci.yml for the actual CI pipeline.
name: Grading (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Grading workflow disabled in portfolio repo"
